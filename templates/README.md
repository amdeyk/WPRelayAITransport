# Starter Templates

Each template directory contains a `page.html` and `page.css` pair that can be copied into a project and deployed with:

```bash
python cli/wrs.py page build --file pages/home.html --css pages-css/home.css --slug home --title "Home"
```

